{% extends 'security/layout.html.twig' %}

{% block title %}Demande de réinitialisation de mot de passe{% endblock %}

{% block content %}
    <section class="h-full relative">
        <div class="p-8 lg:w-1/2 mx-auto h-full flex items-center justify-center">
            <div class="bg-white rounded-lg p-8 relative w-full">
                <h1 class="text-center text-lg font-bold">Email envoyé</h1>
                <p class="text-center text-sm font-medium opacity-70 mt-5">Si un compte correspondant à votre adresse email existe, alors un email vient d'être envoyé contenant un lien que vous pouvez utiliser pour réinitialiser votre mot de passe. Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>
            </div>
        </div>
    </section>
{% endblock %}
