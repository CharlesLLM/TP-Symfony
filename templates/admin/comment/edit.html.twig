{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block content %}
    <h1 class="text-2xl leading-none font-bold mb-5">Commentaire de {{ comment.author.username }}</h1>

    {{ include('admin/comment/_form.html.twig') }}

    <div class="flex justify-between mt-5">
        <a href="{{ path('admin_comment_index') }}" class="form-btn bg-gray-400 hover:bg-gray-500 focus:outline-none focus:ring-2 focus:ring-offset-2">Retour</a>
        {{ include('admin/comment/_delete_form.html.twig') }}
    </div>
{% endblock %}
