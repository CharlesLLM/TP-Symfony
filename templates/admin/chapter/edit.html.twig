{% extends 'base.html.twig' %}

{% block title %}{{ chapter.title }}{% endblock %}

{% block content %}
    <h1 class="text-2xl leading-none font-bold mb-5">{{ chapter.title }}</h1>

    {{ include('admin/chapter/_form.html.twig') }}

    <div class="flex justify-between mt-5">
        <a href="{{ path('admin_chapter_index') }}" class="form-btn bg-gray-400 hover:bg-gray-500 focus:outline-none focus:ring-2 focus:ring-offset-2">Retour</a>
        {{ include('admin/chapter/_delete_form.html.twig') }}
    </div>
{% endblock %}
